# Dependencies for running unit tests in CI
# This file excludes CCX packages that require the internal Nexus repository.
#
# Unlike the hermetic image build, CI runs on stock GitHub runners with only
# public PyPI. The image gets psycopg2 from the python3.12-psycopg2 RPM, while
# PyYAML and charset-normalizer are regular pip dependencies pinned in
# requirements.txt when pulled in transitively. The unit tests use an in-memory
# SQLite engine (tests/conftest.py), so no psycopg driver is needed here.

# FastAPI and web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0

# Insights-core (available on public PyPI)
insights-core>=3.2.26

# HTTP client
httpx>=0.25.0

# Configuration and utilities
pydantic>=2.5.0
pyyaml>=6.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.23.0
